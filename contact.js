const secondDev = document.getElementById("secondDev");

window.addEventListener("scroll", () => {
    const position = secondDev.getBoundingClientRect().top;
    const screenHeight = window.innerHeight;

    if (position < screenHeight - 100) {
        secondDev.classList.add("show");
    }
});

const thirdDev = document.getElementById("thirdDev");

window.addEventListener("scroll", () => {
    const position = thirdDev.getBoundingClientRect().top;
    const screenHeight = window.innerHeight;

    if (position < screenHeight - 100) {
        thirdDev.classList.add("show");
    }
});

const fourthDev = document.getElementById("fourthDev");

window.addEventListener("scroll", () => {
    const position = fourthDev.getBoundingClientRect().top;
    const screenHeight = window.innerHeight;

    if (position < screenHeight - 100) {
        fourthDev.classList.add("show");
    }
});

const fifthDev = document.getElementById("fifthDev");

window.addEventListener("scroll", () => {
    const position = fifthDev.getBoundingClientRect().top;
    const screenHeight = window.innerHeight;

    if (position < screenHeight - 100) {
        fifthDev.classList.add("show");
    }
});

const sixthDev = document.getElementById("sixthDev");

window.addEventListener("scroll", () => {
    const position = sixthDev.getBoundingClientRect().top;
    const screenHeight = window.innerHeight;

    if (position < screenHeight - 100) {
        sixthDev.classList.add("show");
    }
});

const seventhDev = document.getElementById("seventhDev");

window.addEventListener("scroll", () => {
    const position = seventhDev.getBoundingClientRect().top;
    const screenHeight = window.innerHeight;

    if (position < screenHeight - 100) {
        seventhDev.classList.add("show");
    }
});

const eighthDev = document.getElementById("eighthDev");

window.addEventListener("scroll", () => {
    const position = eighthDev.getBoundingClientRect().top;
    const screenHeight = window.innerHeight;

    if (position < screenHeight - 100) {
        eighthDev.classList.add("show");
    }
});

const contactForm = document.getElementById("contactForm");

window.addEventListener("scroll", () => {
    const position = eighthDev.getBoundingClientRect().top;
    const screenHeight = window.innerHeight;

    if (position < screenHeight - 100) {
        contactForm.classList.add("show");
    }
});